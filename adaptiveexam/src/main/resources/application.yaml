# 服务器端口配置
server:
  port: 8081

# Spring 数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 这里的 ahuralearn_adaptiveexam 就是你建的数据库名
    url: jdbc:mysql://localhost:3306/ahuralearn_adaptiveexam?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: lz20050505  # ⚠️ 重要：请把这里改成你安装 MySQL 时设置的真实密码！

# 🌟 MyBatis-Plus 配置（原来写的是 mybatis:，已统一改为 mybatis-plus:）
mybatis-plus:
  configuration:
    # 开启驼峰命名自动映射（下划线 -> 驼峰）
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      # 全局主键策略：ASSIGN_ID = 雪花算法，以 String 形式存入 varchar(50)
      id-type: assign_id

# AI 统一配置 (LangChain4j + 阿里 DashScope)
langchain4j:
  dashscope:
    chat-model:
      api-key: sk-ws-H.EDMDPPL.cP2l.MEQCIB639GX5T9deYcZ5tofzBcmnRphh23YoiRZxUC2CMHzwAiB8doB9gZwWl05IvajZFl21KzNmsKPJlTkaVsijA2uj1Q
      model-name: qwen-turbo